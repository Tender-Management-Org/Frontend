import type { Metadata } from "next";
import { Bento } from "./_components/Bento";
import { Browse } from "./_components/Browse";
import { BeforeAfter } from "./_components/BeforeAfter";
import { DocIntel } from "./_components/DocIntel";
import { Faq } from "./_components/Faq";
import { FinalCTA } from "./_components/FinalCTA";
import { FitScore } from "./_components/FitScore";
import { Footer } from "./_components/Footer";
import { Hero } from "./_components/Hero";
import { Learns } from "./_components/Learns";
import { Nav } from "./_components/Nav";
import { Outcome } from "./_components/Outcome";
import { Problem } from "./_components/Problem";
import { SemanticSearch } from "./_components/SemanticSearch";
import { Workspace } from "./_components/Workspace";
import { WhoItsFor } from "./_components/WhoItsFor";

export const metadata: Metadata = {
  title: "tenderkhoj — AI-powered tender intelligence for Indian businesses",
  description:
    "tenderkhoj discovers government tenders, understands your business, and recommends the opportunities that actually fit your firm — with a personalized Fit Score, AI document intelligence and instant eligibility analysis.",
  keywords: [
    "government tenders India",
    "tender intelligence",
    "e-procurement",
    "AI tender search",
    "GeM tenders",
    "eligibility analysis",
    "bid management",
  ],
  alternates: { canonical: "/landing-2" },
  openGraph: {
    title: "tenderkhoj — The right tenders. For the right firm. At the right time.",
    description:
      "AI-powered tender intelligence: continuous discovery, personalized Fit Scores, document intelligence and eligibility analysis for Indian businesses.",
    type: "website",
  },
  robots: { index: true, follow: true },
};

const STRUCTURED_DATA = {
  "@context": "https://schema.org",
  "@type": "SoftwareApplication",
  name: "tenderkhoj",
  applicationCategory: "BusinessApplication",
  operatingSystem: "Web",
  description:
    "AI-powered tender intelligence platform that discovers government tenders, scores them against your firm with a Fit Score from 0–100, extracts eligibility and document requirements, and manages the complete bidding lifecycle.",
  offers: { "@type": "Offer", price: "0", priceCurrency: "INR" },
  areaServed: "IN",
};

export default function LandingTwoPage() {
  return (
    <>
      <script
        type="application/ld+json"
        // Static, developer-authored JSON — no user input is interpolated.
        dangerouslySetInnerHTML={{ __html: JSON.stringify(STRUCTURED_DATA) }}
      />

      <a
        href="#main"
        className="sr-only focus:not-sr-only focus:fixed focus:left-4 focus:top-4 focus:z-[60] focus:rounded-full focus:bg-stage focus:px-4 focus:py-2 focus:text-sm focus:font-semibold focus:text-white"
      >
        Skip to content
      </a>

      <Nav />

      <main id="main" className="overflow-x-clip bg-canvas text-ink-900 antialiased">
        {/* 2 — Hero + the "thousands of tenders" filter transition */}
        <Hero />

        {/* 3 — The problem, and the smarter way */}
        <Problem />

        {/* 4 — AI matching and the Fit Score */}
        <FitScore />

        {/* 5 — The model learns from your team */}
        <Learns />

        {/* 6 — Document Intelligence */}
        <DocIntel />

        {/* 7 — One workspace, discovery to decision */}
        <Workspace />

        {/* 8 — Semantic search */}
        <SemanticSearch />

        {/* 9 — Feature bento grid */}
        <Bento />

        {/* 10 — Before vs after */}
        <BeforeAfter />

        {/* 11 — The outcome */}
        <Outcome />

        {/* 12 — Who it's for */}
        <WhoItsFor />

        {/* Public browse directories — state / category / sector */}
        <Browse />

        {/* FAQ */}
        <Faq />

        {/* 13 — Final CTA */}
        <FinalCTA />
      </main>

      <Footer />
    </>
  );
}
